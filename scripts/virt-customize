#!/usr/bin/env bash

if command -v virt-customize >/dev/null; then
  command virt-customize "$@"
elif command -v libguestfsw >/dev/null; then
  command libguestfsw virt-customize "$@"
else
  curl -LfsS https://git.io/recordrw | "$SHELL" -s -- virt-customize "$@"
fi
