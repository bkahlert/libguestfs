#!/usr/bin/env bash

if command -v guestfs >/dev/null; then
  command guestfs "$@"
elif command -v libguestfsw >/dev/null; then
  command libguestfsw guestfs "$@"
else
  curl -LfsS https://git.io/recordrw | "$SHELL" -s -- guestfs "$@"
fi
