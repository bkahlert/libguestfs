#!/usr/bin/env recordr

declare tmp
tmp=$(mktemp "${TMPDIR%/}/XXXXXX")
rm "$tmp"
mkdir -p "$tmp"
cp "test/fixture/cir""ros.img" "$tmp/disk.img"
cd "$tmp"

declare cmd
cmd="$PWD/scripts/copy-out"
chmod +x "$cmd"

copy-out() {
  "$cmd" "$@"
}

rec copy-out /home/cirros
ls -lisa
